syntax = "proto3";

option java_multiple_files = true;
option java_outer_classname = "ClientProto";

package GrupoB.gRPCService.ClientProto;

service Client {
    rpc ping (EmptyMessage) returns (BooleanMessage) {}
    rpc store (BlockData) returns (EmptyMessage) {}
    rpc findNode (NodeID) returns (Nodes) {}
}

message EmptyMessage {
}

message BooleanMessage {
    bool result = 1;
}

message BlockData {
    // TODO: Enter a blockchain's block data parameters
}

message NodeID {
    string nodeID = 1;
}

message Nodes {
    repeated NodeInfo nodes = 1;
}

message NodeInfo {
    string address = 1;
    int32 port = 2;
    string nodeID = 3;
}